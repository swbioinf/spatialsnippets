---
title: "Spatial Sampler"
site: workflowr::wflow_site
output:
  workflowr::wflow_html:
    toc: false
editor_options:
  chunk_output_type: console
bibliography: /home/s.williams/zotero_export/MyLibrary.bib
---


Single cell resolution spatial technologies like CosMx, Xenium, vizgen and STOmics have moved paste the technology demo stages, and are now being applied to real world biological research problems. Many [excellent resources](g_tutorials.html) cover the preprocessing spatial data from these platforms, and many more package vignettes cover specific tools. 

Spatial sampler is instead a collection of How-Tos framed around testing specific biological questions – ie comparative tests between experimental conditions. 

Each HowTo includes:

* A **worked example** using a real dataset, which explains why each step is neccessary
* What **input data** is needed
* What the **output** looks like
* A **code snippet** that can be copy-pasted

Explore these HowTos with the searchable table below.


```{r echo=FALSE, warning=FALSE, message=FALSE}
library(reactable)
library(htmltools)
library(dplyr)

howto_info_file <- 'data/howto_info.tsv'
howto_info <- read.delim(howto_info_file, sep="\t")
howto_info$image <- paste0("assets/",howto_info$image)
```


```{r echo=FALSE}
#Defaine column widths;
#https://glin.github.io/reactable/articles/examples.html#column-widths

# reactable leaves col order as is.
data <- select(howto_info, image, howto, Description,  Technology, Toolkit, url, everything() )

reactable(
  data,
  
  searchable = TRUE, # filtering is also possible, but at this stage, a simple search box is enough.
  
  columns = list(
    
    image = colDef(
      name = "",
      minWidth = 250,
      cell = function(value, index) {
        
        image <- img(src = value, 
                     #style = "object-fit: contain",  
                     alt = value
                     ) #
        link_with_image <- tags$a(
            href = data$url[index],
            image
        )
        
        
        tagList(
          tags$a(style = "font-weight: 600; color:black;", href = data$url[index], target = "_blank",  data$howto[index]),
          div(style = "display: inline-block;", link_with_image)

        )
        
      }),
    
    howto       = colDef(show = FALSE),
    
    Description = colDef(minWidth = 200),

    # By default hidden columns aren't searched, even though the content is copied
    # into a different column. 
    # Searchable=TREU  enables search of specific tech, e.g. 'cosmx' to work
    SpecificTech     = colDef(show = FALSE, 
                              searchable = TRUE), 
    

    #Combine broad and specific tech    
    Technology = colDef(
      cell = function(value, index) {
          specificTech <- data$SpecificTech[index]
          div(
            div(value),
            div(style = "font-style: italic", specificTech)
          )
    }),
    
    Toolkit = colDef(minWidth = 50),
    
    
    url = colDef(
      name = "",
      width = 50,
        cell = function(value) {
          tags$a(href = value, target = "_blank",  "▶️")
      }
      
    )
  )
    
)



```

 


![](assets/qcif_logo.png){width=29%} ![](assets/Australian-Biocommons-Logo.png){width=29%} ![](assets/griffith_logo.png){width=29%} 





